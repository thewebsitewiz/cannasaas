import { Routes } from '@angular/router';

/**
 * Real routes (login, AdminLayout shell, page lazy-loads) land in
 * sc-622/sc-623. Scaffold ships with an empty route table so the
 * router-outlet renders nothing and the app boots cleanly.
 */
export const routes: Routes = [];
