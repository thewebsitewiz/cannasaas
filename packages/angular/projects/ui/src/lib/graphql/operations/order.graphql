query Order($orderId: ID!, $dispensaryId: ID) {
  order(orderId: $orderId, dispensaryId: $dispensaryId) {
    orderId
    orderStatus
    orderType
    subtotal
    taxTotal
    discountTotal
    total
    createdAt
    updatedAt
    cancellationReason
    cancelledAt
  }
}
