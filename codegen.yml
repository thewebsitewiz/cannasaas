schema: "http://localhost:3000/graphql"
generates:
  packages/types/src/generated/graphql.ts:
    plugins:
      - typescript
    config:
      scalars:
        DateTime: string
        JSON: Record<string, unknown>
        ID: string
      enumsAsTypes: true
      skipTypename: true
      maybeValue: T | null
